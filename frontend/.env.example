# Copy to frontend/.env.local for local dev (optional — Vite proxy uses /api by default).

# Production / preview: full URL of your Flask API (no trailing slash)
# VITE_API_URL=https://your-api.up.railway.app
